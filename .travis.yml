language: python
python:
  - "3.4"
install:
  - "pip install -r tests_requirements.txt"
script: 
  - coverage run run_tests.py --rcfile=.coveragerc -v && coverage report -m
after_success:
  - coveralls