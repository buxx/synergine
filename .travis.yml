language: python
python:
  - "3.4"
install:
  - "pip install -r tests_requirements.txt"
script: 
  - python3.4 run_tests.py
  - py.test run_tests.py coveralls -v --cov coveralls --cov-report term-missing
after_success:
  - coveralls